{
  "name": "findsl-workspace",
  "private": true,
  "version": "1.2.0",
  "license": "EUPL-1.2",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "langium:generate": "npm -w @findsl/core run langium:generate",
    "embed:runtime": "node scripts/embed-runtime-java.mjs && node scripts/embed-runtime-ts.mjs",
    "embed:mermaid": "node scripts/gen-mermaid-asset.mjs",
    "build": "node scripts/embed-runtime-java.mjs && node scripts/embed-runtime-ts.mjs && node scripts/gen-mermaid-asset.mjs && tsc -b tsconfig.json && node scripts/copy-assets.mjs",
    "watch": "tsc -b tsconfig.json --watch",
    "bundle": "node esbuild.mjs",
    "bundle:watch": "node esbuild.mjs --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "codegen:difftest": "node scripts/codegen-difftest.mjs",
    "dist": "npm run build && npm run bundle",
    "binary": "npm run bundle && node scripts/build-binary.mjs",
    "binary:lsp": "npm run bundle && node scripts/build-binary.mjs lsp",
    "binary:cli": "npm run bundle && node scripts/build-binary.mjs cli",
    "all": "npm run langium:generate && npm run build && npm run bundle && npm test && node scripts/build-binary.mjs all",
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/sync-version.mjs --check"
  },
  "overrides": {
    "dompurify": "3.4.9",
    "@codingame/monaco-vscode-api": {
      "dompurify": "3.4.9"
    }
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/katex": "0.16.8",
    "@types/markdown-it": "14.1.2",
    "@types/node": "24.13.2",
    "@types/pdfmake": "0.3.3",
    "@vitest/coverage-v8": "4.1.8",
    "esbuild": "0.28.0",
    "eslint": "10.4.1",
    "eslint-config-prettier": "10.1.8",
    "globals": "17.6.0",
    "mermaid": "11.15.0",
    "postject": "1.0.0-alpha.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.61.0",
    "vitest": "4.1.8"
  }
}
